# Copia este archivo como  .env  y pega tu token del túnel de Cloudflare.
# El archivo .env NO se sube a GitHub (está en .gitignore).
#
# El token lo obtienes al crear el túnel en el panel de Cloudflare Zero Trust
# (ver DESPLIEGUE.md, sección "Cloudflare Tunnel").
TUNNEL_TOKEN=pega-aqui-tu-token
